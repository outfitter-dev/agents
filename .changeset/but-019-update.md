---
"but": minor
---

Update for GitButler CLI 0.19.0: rename 15 broken commands, add new commands (resolve, squash, show, commit --changes, push --dry-run, config subsystem), and update comparison tables for full CLI parity.
